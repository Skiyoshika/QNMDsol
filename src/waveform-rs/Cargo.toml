[package]
name = "waveform-pipeline"
version = "0.1.0"
edition = "2021"
description = "Extracted time-series display pipeline from OpenBCI GUI, rewritten in Rust."
license = "MIT OR Apache-2.0"

[dependencies]

[dev-dependencies]
eframe = "0.27"
egui_plot = "0.27"
env_logger = "0.11"
rand = "0.8"
